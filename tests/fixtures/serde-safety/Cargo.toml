# `[workspace]` keeps this a standalone workspace root inside the obfuscator's
# own repository.
[workspace]

[package]
name = "serde-safety"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
